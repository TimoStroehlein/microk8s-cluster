tls:
  stores:
    default:
      defaultCertificate:
        certFile: /certs/tls.crt
        keyFile: /certs/tls.key
  options:
    default:
      minVersion: VersionTLS12
      sniStrict: false
  certificates:
    - certFile: /certs/tls.crt
      keyFile: /certs/tls.key
